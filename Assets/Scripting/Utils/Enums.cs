namespace CMGTSA
{
    public enum ActionTypes { Attack, Jump, MoveLeft, MoveRight }
    public enum InputTypes { Mouse, Keyboard }
    public enum Directions { Left, Right, Up, Down, None }
    public enum Animations { Jumping, Idling, Falling, Attack, Dying, Running }
}